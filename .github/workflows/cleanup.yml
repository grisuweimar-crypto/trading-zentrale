name: Watchlist Cleanup
on:
  workflow_dispatch:
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: pip install pandas gspread oauth2client python-dotenv yfinance numpy
      - name: Cleanup ausführen
        env:
          # Wir nutzen NUR DEIN Secret
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
        run: python -c "from utils.cleanup_watchlist import cleanup; cleanup()"